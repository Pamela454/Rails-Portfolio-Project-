<!DOCTYPE html>
<html lang="en">
<head>
<style>
      body {
      background-image: url('<%= asset_path 'camilo-jimenez-vGu08RYjO-s-unsplash.jpg' %>');
      background-size: cover;
      }
      .center {
        justify-content: center;
      }
      .navbar-custom {
        background-color: #D8DEEE;
        justify-content: center;
      }
      .sign-in {
        border: 0;
        border-radius: 1rem;
        box-shadow: 0 0.5rem 1rem 0 rgba(0, 0, 0, 0.1);
        background-color: #D8DEEE;
      }
      .card-signin .card-title {
        margin-bottom: 2rem;
        font-weight: 300;
        font-size: 1.5rem;
      }
      .card-signin .card-body {
        padding: 2rem;
      }

</style>
</head>
<body>
  <div class="text-center center font-weight-bold">
  <nav class="navbar navbar-custom bg-dark text-white sticky-top center">
  <div class="col">
  <h1>Welcome to Ask A Health Question</h1>
    <% if flash[:notice] %>
    <div class="notice"><h2><%= flash[:notice] %></h2></div>
    <% end %>
  </nav>
 </div>
  </div>
  </div>
</div>

<div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="card bg-dark text-white" style="border-radius: 1rem;">
          <div class="card-body p-5 text-center">

            <div class="mb-md-5 mt-md-4 pb-5">
            <%= form_for(:session, url: "users/signin", html: { autocomplete: "new-password" })   do |f| %>
              <h2 class="fw-bold mb-2 text-uppercase">Sign in if you are a Member</h2>

              <div class="form-outline form-white mb-4">
                <div class="form-label"><%= label :email, "Enter your email"%>
                  <%= f.text_field :email, :class => "form-control form-control-lg" %>
                </div>
              </div>

            <div class="form-outline form-white mb-4">
              <div class="form-label"><%= label :password, "Enter your password" %>
                <%= f.password_field :password, :class => "form-control form-control-lg"  %>
              </div>
            </div>

              <%= f.submit "Sign In" , :class => "btn btn-outline-light btn-lg px-5" %>

              <div class="d-flex justify-content-center text-center mt-4 pt-1">
              <%= button_to "Login With Facebook", "/auth/facebook", method: :post %>
              </div>
            <% end %>

            </div>

            <div>
              <p class="mb-0">Need to sign up as a Patient user? <a href="users/new_patient" class="text-white-50 fw-bold">Sign Up</a>
              </p>
              <p class="mb-0">Need to sign up as a Physician user? <a href="users/new_physician" class="text-white-50 fw-bold">Sign Up</a>
              </p>
            </div>

          </div>
        </div>
      </div>
    </div>

<!DOCTYPE html>
<html lang="en">
  <style>
    body {
    	  background-image: url(<%=asset_path('camilo-jimenez-vGu08RYjO-s-unsplash.jpg')%>)!important;
        background-repeat: no-repeat;
        margin: 0;
        background-size: cover;
        overscroll-behavior: none;
        overflow-x: hidden;
        margin-top: 175px;
    }
  </style>
  <body>
    <div class="row d-flex justify-content-center">
      <div class="card bg-dark text-white" style="border-radius: 1rem;">
        <h2 class="card-header fw-bold text-center">Sign in if You Are a Member</h2>
        <%= form_for @user, :url => {:controller => 'sessions', :action => 'create'} do |f| %>
          <div class="card-body p-3 text-center">
            <div class="form-group">
              <div class="input-group-lg mb3">
                <div class="form-outline form-white mb-4">
                  <div class="form-label"><%= label :email, "Enter your email"%>
                    <%= f.text_field :email, value: @user.email, :class => "form-control form-control-lg" %>
                  </div>
                </div>
              </div>
              <div class="form-group">
                <div class="input-group-lg mb-3">
                  <div class="form-outline form-white mb-4">
                    <div class="form-label"><%= label :password, "Enter your password" %>
                      <%= f.password_field :password, value: @user.password, :class => "form-control form-control-lg"  %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <%= f.submit "Sign In" , :class => "btn btn-outline-light btn-lg px-5" %>
          <% end %>
        </div>
        <div class="card-footer text-center">
          <div class="row ml-2">
            <p class="text-center">Need to sign up as a Patient? <a href="users/new_patient" class="ml-2 text-white-50 fw-bold">Sign Up</a></p>
          </div>
          <div class="row ml-2">
            <p class="text-center">Need to sign up as a Physician? <a href="users/new_physician" class="ml-2 text-white-50 fw-bold">Sign Up</a></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="container-fluid">
  <div class="d-flex justify-content-center text-center mt-0 pt-1">
    <%= button_to 'Login With Facebook', '/auth/facebook', :class => 'btn btn-outline-dark mb-5', method: :post %>
  </div>
</div>
</div>
</body>
